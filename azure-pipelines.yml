trigger:
- main

pool:
  name: automation-agent-sebas

steps:
- checkout: self

- script: |
    echo Build Sources Directory: %BUILD_SOURCESDIRECTORY%
    cd %BUILD_SOURCESDIRECTORY%
    dir
    .\gradlew.bat clean test
  displayName: 'Run Gradle tests (Windows)'