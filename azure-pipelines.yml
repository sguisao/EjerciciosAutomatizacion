trigger:
- main

pool:
  name: automation-agent-sebas

steps:
- checkout: self

- script: |
    echo "Running Gradle tests"
    chmod +x ./gradlew
    ./gradlew clean test
  displayName: 'Run Gradle tests'